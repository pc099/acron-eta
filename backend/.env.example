# ── Database ──────────────────────────────────
DATABASE_URL=postgresql+asyncpg://asahi:asahi_dev_password@localhost:5432/asahi

# ── Redis ─────────────────────────────────────
REDIS_URL=redis://localhost:6379

# ── Clerk Auth ────────────────────────────────
CLERK_SECRET_KEY=sk_test_...
CLERK_PUBLISHABLE_KEY=pk_test_...

# ── CORS (frontend origin; required in production) ─
# Single: CORS_ORIGINS=https://your-app.vercel.app
# Multiple: CORS_ORIGINS=https://app.vercel.app,https://custom.com
# Or JSON:  CORS_ORIGINS=["https://your-app.vercel.app"]
CORS_ORIGINS=["http://localhost:3000"]

# ── Debug: true = mock responses (no keys); false = real inference (set keys below) ─
DEBUG=true

# ── LLM Providers (required when DEBUG=false for real /v1/chat/completions) ─
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# ── Stripe (optional, billing not yet implemented) ──
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
