# ── Core Framework ──────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0

# ── LLM Providers ──────────────────────────────────────
anthropic>=0.40.0
openai>=1.55.0

# ── Configuration ──────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0.2

# ── HTTP Client (for embeddings & external APIs) ──────
httpx>=0.27.0

# ── Event Streaming (optional, enable via config) ─────
kafka-python>=2.0.2

# ── Testing ────────────────────────────────────────────
pytest>=8.3.0
pytest-cov>=6.0.0
pytest-asyncio>=0.24.0

# ── Code Quality ───────────────────────────────────────
black>=24.10.0
ruff>=0.8.0
mypy>=1.13.0

# ── Numeric / Embeddings (needed by Phase 2+) ─────────
numpy>=1.26.0
